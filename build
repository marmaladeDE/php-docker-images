#!/bin/bash

if [ -z "$1" ]; then
	echo "missing version"
	exit
fi

exec docker build \
	--pull \
	--tag makaira/php:$1 \
	--force-rm \
	--memory 4096MB \
	--memory-swap 4096MB \
	-f Dockerfile-$1 \
	.
