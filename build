#!/bin/bash

if [ -z "$1" ]; then
	echo "missing version"
	exit
fi

exec docker build \
	--pull \
	--tag localhost:5000/makaira/php:${PHP_VERSION} \
	--force-rm \
	--memory 4096MB \
	--memory-swap 4096MB \
	-f Dockerfile-$1 \
	.
